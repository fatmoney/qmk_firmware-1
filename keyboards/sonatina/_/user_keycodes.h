enum {
    _ = KC_TRANSPARENT,

    G_ENT = MT(MOD_LGUI, KC_ENT),

    C_A = MT(MOD_LCTL, KC_A),

    S_DOT = MT(MOD_LSFT, KC_DOT),
    S_Z = MT(MOD_LSFT, KC_Z),
    S_ENT = MT(MOD_LSFT, KC_ENT),

    L1_SPC = LT(1, KC_SPC),
    L2_ENT = LT(2, KC_ENT),

    SS = G(S(KC_4)), // Screen Shot (macOS)
    EISU = KC_LNG2,
    KANA = KC_LNG1,
    BACK = G(KC_LBRC) // Go back in the browser
};
